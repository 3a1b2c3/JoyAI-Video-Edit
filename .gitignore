# Vendored weights, checkpoints, and compile cache (~117G, not for git)
deploy/deps/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.ipynb_checkpoints/

# OS / editor
.DS_Store

# Runtime artifacts (recordings, server logs)
deploy/recordings/
deploy/server.log

# Local secrets / machine-specific launch settings
.env
.env.*
!.env.example
